# WeatherCast Screens

Add the app screenshots to this folder using these exact filenames:

- `home.png`
- `hourly-forecast.png`
- `weather-map.png`
- `city-search.png`
- `saved-locations.png`
- `language-selection.png`
- `location-permission.png`
- `offline-mode.png`
- `arabic-layout.png`

The root `README.md` already references these files, so the screens will appear automatically after the images are added.
